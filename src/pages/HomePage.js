import styled from "styled-components";

const StyledPage = styled.div`
  
`;

const HomePage = () => {
  return (
    <StyledPage>
      Home Page
    </StyledPage>
  );
};

export default HomePage;
